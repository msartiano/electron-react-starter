import React from 'react';
import AmazonList from './components/AmazonList';

export default () => {
    return (
        <div>
            <AmazonList />
            React has initialised correctly.
        </div>
    );
};